server/*.rbc
server/capybara-*.html
server/.rspec
server/db/*.sqlite3
server/db/*.sqlite3-journal
server/db/*.sqlite3-[0-9]*
server/public/system
server/coverage/
server/spec/tmp
server/*.orig
server/rerun.txt
server/pickle-email-*.html

# Ignore all logfiles and tempfiles.
server/log/*
server/tmp/*
!server/log/.keep
!server/tmp/.keep

# TODO Comment out this rule if you are OK with secrets being uploaded to the repo
server/config/initializers/secret_token.rb
server/config/master.key

# Only include if you have production secrets in this file, which is no longer a Rails default
# config/secrets.yml

# dotenv
# TODO Comment out this rule if environment variables can be committed
server/.env

## Environment normalization:
server/.bundle
server/vendor/bundle

# these should all be checked in to normalize the environment:
# Gemfile.lock, .ruby-version, .ruby-gemset

# unless supporting rvm < 1.11.0 or doing something fancy, ignore this:
server/.rvmrc

# if using bower-rails ignore default bower_components path bower.json files
server/vendor/assets/bower_components
server/*.bowerrc
server/bower.json

# Ignore pow environment settings
server/.powenv

# Ignore Byebug command history file.
server/.byebug_history

# Ignore node_modules
server/node_modules/

# Ignore precompiled javascript packs
server/public/packs
server/public/packs-test
server/public/assets

# Ignore yarn files
server/yarn-error.log
server/yarn-debug.log*
server/.yarn-integrity

# Ignore uploaded files in development
server/storage/*
!server/storage/.keep

server/app/models/concerns/.keep
server/app/controllers/concerns/.keep
server/config/credentials.yml.enc

server/test/*/.keep
